

1. left, right :  up() 메서드 연결
2. 버블 하나가 다 쓰레드 계속 만들어면 느려짐 (컴퓨터 성능에 따라 비교 됨) 
3. 충돌 감지 만들기 (외벽 상단 벽 감지) 
4. 백그라운드 서비스를 버블에서 설정하게 되면 더 느려 진다. 
5. 버블(스레드) + 버블옵저버(스레드) 만들게 된다면 x 2 (안 좋음) 
6  버블(스레드) + 전체버블옵저버(스레드) --> arrayList 다 넣고 하나씩 
   반복문 돌면서 계속 체크 해야 함. 
